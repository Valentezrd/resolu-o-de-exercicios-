#include <iostream>

using namespace std;

int
main ()
{
  float a;
  cin>>a;
  if (a > 0)
    {
      cout << 1;
    }
  if (a < 0)
    {
      cout << -1;
    }

  if (a == 0)
    {
      cout<<"0";
    }
  return 0;
}
